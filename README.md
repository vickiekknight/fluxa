# fluxa
A robotics simulation tool that makes it easier to generate synthetic data. 

This is version 2, where I used the Dedalus SDK to connect an agent to an Isaac Sim MCP. This version is able to generate multiple robot types, solid objects, and environments like rooms and warehouses.
